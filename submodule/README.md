# Submodule Reference

This directory is used to store external repositories as Git Submodules.

> **Important Notice / Disclaimer:**
> Code and modules within the `submodule/` directory (such as `fumadocs`) are provided **strictly for internal reference and architectural study**. The main project has no direct dependencies, component affiliations, or integration ties with the code contained in this directory.
